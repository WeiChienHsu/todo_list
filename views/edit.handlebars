<div class = "container">
<h1>Edit Task:</h1>
    <form id="updateTask" action="/notes/{{task.id}}/?_method=PUT" method="POST">
    <div class="form-group">
        <label for="fname">Title:</label>
        <input class="form-control" type="text" name="titleInput" value="{{task.title}}" required>
    </div>
    <button class="form-control btn btn-default btn-success" type = "submit">Update</button> 
    </form>
</div>